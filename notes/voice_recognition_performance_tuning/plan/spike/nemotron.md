# S8 `nemotron` — Nemotron 3.5 に差し替えて比べる

## 1. このセッションへの指示

- このセッションでやるのは **S8 だけ**。
- **まず飛ばす条件を確認する。** S6 `sensevoice` と S7 `qwen3` の**両方で日本語が明確に落ちていたら、この spike は実施しない。** その判断を記録に書いて終わる。
- この候補だけは**認識器の種類が変わる**（オフライン → 逐次型）。実装の変更が他の2つより大きい。**その大きさも観察項目である。**
- **`sherpa_onnx` の master（未リリースの実装）を取り込まない。** 用語登録とビームサーチはリリース版では使えない。使おうとすると**プロセスが終了する**。
- **人の手数を増やす作りにしない**（`master.md` §1 の原則）。とくに次をさせない。
  - **モデルの取得と配置を人の作業にしない。** スクリプト1本にし、人は1コマンド叩くだけにする
  - **全件の出力を読ませない。** 差分が出た行だけを見せる
  - **言語指定の有無を切り替えるたびに再インストールさせない。** 画面から切り替えられるようにする
- **英語混じりは「表記が定まるか」で見る。** S3 で、現行モデルの問題は「英語が出ない」ではなく「**表記が定まらない**」と分かった。この候補は大文字と句読点に対応するとされているので、**その主張が実機で確かめられる唯一の候補**である。
- 結果は `notes/voice_recognition_performance_tuning/spike/nemotron.md` に書く。**`main` にはマージしない。**

## 2. 背景

### 仮説

言語の品質区分が優先順位と一致する（日本語＝最上位・英語＝最上位・**普通話＝二番手**）。句読点と大文字にも対応する。

### 確認済みの制約（`research/opus_repropose_v3.md` §1-B-1、§1-B-2）

- **リリース版（1.13.3 も 1.13.8 も）では、ビームサーチも用語登録も使えない。** 該当実装は `greedy_search` 以外を受け取ると異常終了する。実装は master にのみある
- **展開後は約650MB で、現行の625MB より大きい**（453MB は圧縮値）
- 発話ごとの**言語指定は現行版でも動く経路がある**（`OnlineStream.setOption` で `language` を渡す）

### 答える問い

- 日本語は現行と比べてどうか（**逐次型なので、発話全体を見てから決める現行より落ちる可能性がある**）
- 日英混在はどう出るか。**発話ごとの言語判定と、1文の中の混在は別の話**である
- 普通話は実用になるか（二番手の区分なので、期待を上げない）
- オフラインから逐次型へ移す実装は、どのくらいの大きさか

## 3. 開始条件と依存

- ブランチ: **S4 `lib-update` のブランチから枝を切る**（足場と 1.13.8 が揃っているため）。名前は `spike/voice_recognition_performance_tuning/nemotron`。**足場を作り直さない**（`master.md` §1）
- **計画書は `main` の最新版を読む。** 枝元のコピーは古い可能性がある。枝を切った直後に `git checkout main -- notes/voice_recognition_performance_tuning/plan` で取り直す（`master.md` §1）
- **記録は spike が終わったら `main` に写す。** ファイルを1つずつ指定して写し、ブランチごとマージしない（`master.md` §1）
- **依存**
  - `spike/current-baseline.md` と、**端末内の音声・`answers.tsv`**（正本は端末。`.dev_models/spike_audio/` はバックアップ）
  - `spike/lib-update.md` と、**S4 の「丸ごと」経路の結果 TSV**（比較の基準。**録音時の結果を基準にしない**）
  - **`spike/sensevoice.md` と `spike/qwen3.md`**（飛ばす条件の判定に要る）
  - **欠けていたら、このセッションはここで止める**
- 前提: **Android 実機**（`master.md` §1）
- **飛ばす条件**: S6 と S7 の両方で日本語が明確に落ちていたら実施しない

## 4. 作業の分担

| 誰が | 何を |
|---|---|
| エージェント | 飛ばす条件の判定、枝の作成、**モデル取得・配置スクリプトの用意**、逐次型認識器への置き換え、固定音声での実行、突き合わせ、実装量の所感、記録の下書き |
| **人** | **スクリプトの実行（1コマンド）**、実機への再インストールの承認、差分が出た行の読み比べ |
| 止まる条件 | モデルの取得と配置（通信量が大きいので実行は人に確認してから）。**人が手で置く作業は無い** |

## 5. エージェントが行う手順

1. **最初に飛ばす条件を判定する。** `spike/sensevoice.md` と `spike/qwen3.md` を読み、両方で日本語が明確に落ちていれば、**ここで終わる**（記録に理由を書く）。
2. S4 のブランチから枝を切る（`sherpa_onnx` は 1.13.8 のまま）。
3. S3・S4 の記録と、端末内の音声・`answers.tsv` があることを確認する。
4. 足場（固定 wav を流す dev 経路）はブランチに入っている。**作り直さない。** ただし**この候補は認識器の種類が違う**ため、足場の中に逐次型で流す入口を足すことになる（丸ごと／VAD 経由の考え方は変えない）。
5. **モデルの取得と配置をスクリプト1本にする。** 対象は `sherpa-onnx-nemotron-3.5-asr-streaming-0.6b-1120ms-int8-2026-06-11`。
   - **1120ms 版を使う。** チャンクが大きいほど精度が高いと公式が書いており、この spike では精度を見たいため
   - 中身は `encoder.int8.onnx` / `decoder.int8.onnx` / `joiner.int8.onnx` と `tokens.txt` の**3ファイル構成**
   - 取得・配置は S6・S7 と同じ流儀（`scripts/download_nemo_model.sh` と `scripts/lib/spike_device.sh`）。**空き容量の確認もスクリプトの中で行う**
   - **`SttModelProvisioner` 本体は変更しない**
6. **認識器を逐次型へ置き換える。** これは他の2つと違い、**クラスの種類が変わる**。
   - 現行の `lib/stt/stt_audio_worker.dart` は `OfflineRecognizer` / `OfflineRecognizerConfig` / `OfflineModelConfig` を使い、発話ごとに `createStream()` している
   - Nemotron では `OnlineRecognizer` / `OnlineRecognizerConfig` / `OnlineModelConfig` / `OnlineTransducerModelConfig`（`encoder` / `decoder` / `joiner`）を使う
   - **`decodingMethod` は `greedy_search` のままにする。** 他の値を入れるとプロセスが終了する
   - 言語の指定は `OnlineStream` の `setOption` に `language` を渡す。値の書き方（`ja` か `ja-JP` か）は**着手時に確認する。想像で書かない**
   - 入力の与え方、確定のタイミング、ストリームの使い回し方は、**着手時に `~/.pub-cache/hosted/pub.dev/sherpa_onnx-1.13.8/lib/src/online_recognizer.dart` と `online_stream.dart` を開いて確認する**
   - **この spike では、本番のパイプラインを逐次型に作り替えない。** 固定 wav を流す dev 経路の中だけで動かせばよい
7. **人に §6 を依頼する。ここで止まる。**
8. モデルが置かれたら、S3 と同じ音声を流し、区分ごとに出力をファイルへ書き出す。区分は `answers.tsv` の `category` 列から取る。**言語指定あり（`ja`）と自動の両方**を試す（**画面から切り替える**）。
9. **同じ音声を2回流す。** 逐次型は状態を持つため、**ストリームの使い回し方によって結果が変わりうる**。変わるなら、その条件も記録する。
10. 現行の出力（S4 の「丸ごと」経路の結果）と1件ずつ突き合わせ、**差分が出た行だけを人に見せる**。
11. **実装の大きさを所感として残す。** どのクラスを置き換えたか、状態の持ち方がどう変わったか、本番に入れるとしたら何が追加で要りそうか。**採用判断の材料になる。**

## 6. 人が行う手順

1. **スクリプトを1つ実行する**（圧縮 450MB 前後・展開 650MB 前後のダウンロードから端末への配置まで自動）
2. 実機への再インストールの承認と操作
3. **差分が出た行だけ**を渡されるので、それを読んで次を返す

| 返してほしいもの | 形 |
|---|---|
| 日本語の読み返しやすさ | 現行と比べて。**実際の出力を引用して** |
| 英語混じりの見え方 | **1文の中で英語が英字のまま出たか**。発話ごとに言語が切り替わっただけか。**同じ語で表記が揺れていないか** |
| 句読点と大文字 | 出ているか |
| 普通話 | 読めるレベルか |

## 7. 観察・記録の枠

`notes/voice_recognition_performance_tuning/spike/nemotron.md` に写す表。

### 言語ごとの結果（**平均して1つの順位にしない**）

| 区分 | 現行の出力 | Nemotron（`ja` 指定） | Nemotron（自動） | 人の判断 |
|---|---|---|---|---|
| 日本語のみ | 実用的（S3） | | | |
| 日本語＋英語 | 地の文は実用的、英単語だけ壊れる（S3） | | | |
| 英語 | **崩壊**（34.7 秒 → `そうニ`） | | | |
| 普通話 | **崩壊**（漢字が出ずカタカナの音写） | | | |

### 英語の表記が定まるか（課題②の本題。**この候補は大文字に対応するとされている**）

| 語 | 現行の出力（複数の表記を並べる） | Nemotron 1回目 | Nemotron 2回目 | 定まったか |
|---|---|---|---|---|
| `I think` | `iiceting` / `iicsinck` / `ieteink` ほか | | | |

### 動作

| 項目 | 現行 | Nemotron |
|---|---|---|
| 1発話あたりの処理時間 | | |
| 常駐RAM の最大 | | |
| 異常終了 | | |
| 句読点・大文字 | | |

### 実装の大きさ（採用判断の材料）

| 項目 | 所感 |
|---|---|
| 置き換えたクラス | |
| 状態の持ち方の変化 | |
| 本番に入れるなら追加で要りそうなもの | |
| 体感の作業量 | |

## 8. やらないこと

- **master の未リリース実装の取り込み**（用語登録・ビームサーチ）
- `decodingMethod` に `greedy_search` 以外を入れること（プロセスが終了する）
- 本番パイプラインの逐次型への作り替え
- 配布設計、準備ゲート、画面の作り込み
- `SttModelProvisioner` の本番の契約の変更
- 「同系統のモデルだから省電力」という前提での評価
- **モデルの取得・配置を人の手作業にすること**
- **録音時の結果（`captured.tsv`）を比較の基準にすること**

## 9. 完了条件と、次へ渡すもの

**完了条件**: 次のどちらか。

- 飛ばす条件に当たり、**実施しない理由が記録に残っている**
- 言語ごとの結果と、**実装の大きさの所感**が記録に残っている

**次へ渡すもの**: `spike/nemotron.md` のみ。**コードはマージしない。**

ここまでで、モデル差し替えの3候補が出そろう。**採用の判断はしない。** `main` に戻ってから、全記録を見て決める。
